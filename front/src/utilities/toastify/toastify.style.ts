import { createStyles } from "../../styles/createStyle";

export const useStyles = () => {
  return createStyles({
    errorToastify: {
      width: "28rem",
      color: "#c03e3d",
      fontFamily: "inherit",
      backgroundColor: "#ecc8c5",
    },
  });
};
