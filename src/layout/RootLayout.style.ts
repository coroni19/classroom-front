import { createStyles } from "../styles/createStyle";

export const useStyles = () =>
  createStyles({
    main: {
      display: "flex",
      marginTop: "160px",
      justifyContent: "center",
    },
  });
