import { createStyles } from "../styles/createStyle";

export const useStyles = () =>
  createStyles({
    main: {
      display: "flex",
      marginTop: "10rem",
      overflowX: "hidden",
      justifyContent: "center",
    },
  });
